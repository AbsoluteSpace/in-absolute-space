## You are now in Absolute Space

https://inabsolutespace.com

## Running locally

- `bundle install` — install dependencies
- `bin/jekyll serve` — start a local dev server at http://localhost:4000
- `bin/jekyll build` — build the static site into `_site/`
